const top = 0
const left = 0
const strokeWidth = 0.5
const radius = 14

const fill = '#fff'
const stroke = '#222'

export default {
  line: {
    coordinates: [250, 125, 250, 175],    
    fill,
    stroke,
    strokeWidth,
    originX: 'center', 
    originY: 'center',
  },
  circle: {
    top,
    left,
    strokeWidth,
    radius,
    fill,
    stroke,
    originX: 'center', 
    originY: 'center',
  }
}